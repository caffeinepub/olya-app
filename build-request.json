{
  "kind": "build_request",
  "title": "Fix: New sessions cannot be created",
  "priority": "high",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Investigate and fix the bug preventing new conversation sessions from being created. Identify whether the failure originates in the backend `createSession` call, the React Query mutation in `useQueries.ts`, or the session creation handler in `Dashboard.tsx`, and restore full session creation functionality.",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-4"
        ],
        "quotes": [
          "New Sessions cannot be created. Fix this problem"
        ]
      },
      "acceptanceCriteria": [
        "Clicking the 'New Session' (or equivalent) button in the SessionManager successfully creates a new session.",
        "The newly created session appears immediately in the session list without requiring a page refresh.",
        "The newly created session becomes the active session.",
        "No errors are thrown in the browser console or backend logs when creating a session.",
        "Session creation works for both authenticated and guest users if both flows are supported."
      ]
    }
  ],
  "constraints": [
    "Do not alter the app language selection feature that was just fixed.",
    "Do not break existing session listing, selection, or deletion functionality."
  ],
  "nonGoals": [
    "Redesigning the session management UI.",
    "Adding new session fields or metadata beyond what already exists.",
    "Changes to transcript entry creation or deletion."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}