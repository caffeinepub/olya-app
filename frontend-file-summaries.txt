{
  "frontend/src/App.tsx": "Root app component with routing, auth flow, and profile setup; imports `useEnsureUserRole` from `useQueries` (restored) and assigns the user role on login.",
  "frontend/src/components/AppHeader.tsx": "Application header updated to include a Palette icon button that opens a Popover containing the ThemeSettingsPanel, positioned between the User Manual button and the language selector.",
  "frontend/src/components/LoginButton.tsx": "Internet Identity login/logout button component with loading state and query cache clearing on logout.",
  "frontend/src/components/ProfileSetupModal.tsx": "Profile setup modal dialog that now accepts optional `onSave` and `isSaving` props instead of managing its own mutation, allowing the parent (App.tsx) to control the save logic.",
  "frontend/src/components/SessionManager.tsx": "Session list sidebar component showing all sessions with timestamps, previews, pattern/violation badges, and always-visible delete (Trash2) buttons inline in the title row; the delete button stops event propagation to prevent accidental session selection and shows a spinner while deleting.",
  "frontend/src/pages/Dashboard.tsx": "Main dashboard page rebuilt to use the actual `useDashboardState()` API (`transcriptEntries`, `beliefState`, `latestEntry`, `addTranscriptEntry`, `toggleTranslation`, `translateEntry`, `clearEntries`), pass correct props to all panels (`SessionSummaryBar`, `TranscriptPanel`, `EmotionIntentPanel`, `BeliefStatePanel`, `SafetyQualityPanel`, `StrategyEnginePanel`), and include `AppHeader`, `TranscriptInput`, and `SemanticAnalysisPanel`.",
  "frontend/src/lib/utils.ts": "Utility module exporting the cn() class name merging helper using clsx and tailwind-merge.",
  "frontend/src/hooks/useSpeechRecognition.ts": "Extended speech recognition hook supporting three ASR engine modes — 'webSpeech' (native Web Speech API), 'whisper' (client-side simulation with Whisper-style processing status), and 'deepspeech' (DeepSpeech acoustic model simulation) — all returning transcript text via the same callback interface.",
  "frontend/src/components/TranscriptInput.tsx": "Transcript input component that calls `useSpeechRecognition` with a proper options object `{ asrEngine, onFinalResult }` matching the hook's `UseSpeechRecognitionOptions` interface, and detects language on both typed and spoken input.",
  "frontend/src/hooks/useDashboardState.ts": "Dashboard state hook that correctly reads `ethicsResult.flags` (the actual field on `EthicsResult`) instead of the non-existent `toxicityFlags`, converting the string array to typed `ToxicityFlag` objects with a default confidence of 0.8.",
  "frontend/src/components/TranscriptPanel.tsx": "Transcript panel with language detection badges and per-entry translation toggle; uses `language` from `useAppLanguagePreference` with null-safe fallback to `'en'`.",
  "frontend/src/components/EmotionIntentPanel.tsx": "Emotion and intent analysis panel using typed `EmotionScore` and `IntentScore` objects with `.emotionType`/`.intentType`/`.confidence` fields; uses `new Date(timestamp).toLocaleTimeString` correctly.",
  "frontend/src/components/BeliefStatePanel.tsx": "Canvas-based belief state graph using typed `TranscriptEntry` and `BeliefState` from `useDashboardState`; removed incorrect imports of `BeliefStateData` and `SPEAKER_COLORS` from other modules.",
  "frontend/src/components/SemanticAnalysisPanel.tsx": "Semantic analysis panel updated to use typed `EmotionScore`, `IntentScore`, and `ToxicityFlag` objects from `useDashboardState`; shows latest entry analysis and session-wide distributions.",
  "frontend/src/components/SessionSummaryBar.tsx": "Session summary bar computing dominant emotion, top strategy, and bias incidents from typed `TranscriptEntry` arrays using `.emotionType`, `.intentType`, and `.strategy` object fields.",
  "frontend/src/components/StrategyEnginePanel.tsx": "LLaMA+RL strategy engine panel that generates strategies from transcript entries and saves the top strategy via `useAddStrategyRecommendation` using `mutate` (fire-and-forget) with a plain string, matching the updated hook signature.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; re-adds `useEnsureUserRole`, exposes `isReady` from `useCreateSession`, and fixes `useAddStrategyRecommendation` to accept a plain `strategy: string` (converting it to a `ConversationPattern` internally) to resolve the type mismatch in `StrategyEnginePanel`.",
  "frontend/src/data/languages.ts": "Complete BCP 47 language data file exporting 300+ Language objects (code, name, nativeName) covering all globally recognized languages and regional variants from A–Z, including all major world languages with their regional sub-variants, plus a getLanguageByCode helper. The list now includes the full tail of languages (Persian, Polish, Portuguese, Quechua, Romanian, Russian, Samoan, Scottish Gaelic, Serbian, Shona, Sindhi, Sinhala, Slovak, Slovenian, Somali, Spanish with all Latin American variants, Swahili, Swedish, Tagalog, Tajik, Tamil, Tatar, Telugu, Thai, Tigrinya, Turkish, Turkmen, Ukrainian, Urdu, Uzbek, Vietnamese, Welsh, Wolof, Xhosa, Yiddish, Yoruba, Zulu, and more) that were previously truncated/missing.",
  "frontend/src/hooks/useLanguagePreference.ts": "Custom React hook that reads and writes the user's transcription language preference to localStorage (key: 'olya-transcription-language'), defaulting to 'auto', and exposes language, setLanguage, and clearLanguage.",
  "frontend/src/components/LanguageSelector.tsx": "Searchable language selector component using Popover + Command UI, listing 'Auto-detect' as the first option followed by all BCP 47 languages with native name and code display; shows a detected-language badge when auto-detect is active and a language has been identified.",
  "frontend/src/utils/ethicsSimulator.ts": "Extended ethics analysis utility that now detects four bias categories (gender, racial, socioeconomic, confirmation) with pattern matching, returning a BiasCategory array alongside existing toxicity/bias flags; backward-compatible with existing EthicsResult interface.",
  "frontend/src/utils/hallucinationGuard.ts": "New hallucination guardrail utility that detects fabricated specificity signals (unsupported research claims, invented statistics, unverifiable named references) and returns a HallucinationResult with flagged status, confidence score, and suspect phrases array.",
  "frontend/src/utils/ethicalConstraints.ts": "New ethical constraints enforcement utility that detects four violation types (personal-attack, manipulative-framing, dehumanizing-language, coercive-pressure) using regex patterns, returning an EnforcementResult with isViolation, violationType, and sanitizedText.",
  "frontend/src/utils/patternPredictionEngine.ts": "Pattern prediction engine updated to access `emotionType` and `intentType` from typed `EmotionScore`/`IntentScore` objects; exports `predictPatterns`, `buildPatternProfile`, and the `PatternProfile` interface.",
  "frontend/src/utils/trustworthinessScoreCalculator.ts": "New trustworthiness score calculator that computes a 0-100 score by applying weighted penalties for bias incidents, ethical violations, and hallucination flag rates.",
  "frontend/src/components/EthicsBadge.tsx": "Extended EthicsBadge component that displays bias category labels and ethical violation types; fixes the TypeScript error by computing the status label before rendering instead of calling formatViolationType with a potentially-undefined value inline.",
  "frontend/src/components/PatternPredictionsPanel.tsx": "Pattern predictions panel accepting optional `profile` prop (defaulting to `buildPatternProfile(entries)`) and rendering four prediction cards with direction indicators and hallucination risk badges.",
  "frontend/src/components/SafetyQualityPanel.tsx": "Safety and quality metrics panel computing bias incidents, flag rate, ethical violations, and trustworthiness score from typed `TranscriptEntry` arrays with `ToxicityFlag` objects.",
  "frontend/src/hooks/useAsrEnginePreference.ts": "New custom hook that persists and retrieves the user's preferred ASR engine mode ('webSpeech', 'whisper', or 'deepspeech') in localStorage, defaulting to 'webSpeech', with setAsrEngine and getAsrEngine helpers.",
  "frontend/src/utils/llamaRLEngine.ts": "LLaMA+RL strategy engine rewritten to use the typed `EmotionScore`/`IntentScore` object fields (`emotionType`, `intentType`, `confidence`) from `TranscriptEntry` instead of string arrays.",
  "frontend/src/utils/strategySimulator.ts": "Strategy simulator that delegates to `generateLlamaRLStrategies` using a minimal fake `TranscriptEntry` with the correct typed shape; exports both `generateStrategies` and `generateStrategiesFromEntries`.",
  "frontend/src/index.css": "Global styles with the existing Ocean Teal design tokens plus three new theme overrides (Midnight Slate, Ember Amber, Forest Green) defined via `[data-theme]` attribute selectors, each with both light and dark mode variants for all CSS custom properties.",
  "frontend/src/hooks/useAppLanguagePreference.ts": "Module-level singleton for app language preference that uses a Set of typed listeners to broadcast language changes to all hook instances; exports `language`, `appLanguage` (alias), and `setAppLanguage` for backward compatibility with all consumers.",
  "frontend/src/components/AppLanguageSelector.tsx": "Full-screen and compact app language selectors now listing 41 languages total (original 9 plus 32 new global languages including Hindi, Bengali, Urdu, Swahili, Turkish, Vietnamese, Thai, Indonesian, Malay, Polish, Ukrainian, Dutch, Romanian, Greek, Czech, Hungarian, Hebrew, Persian, Tagalog, Hausa, Yoruba, Amharic, Burmese, Khmer, Somali, Zulu, Serbian, Croatian, Slovak, Bulgarian, Catalan, and Punjabi), with searchable lists and native name display in both components.",
  "frontend/src/pages/UserManual.tsx": "Comprehensive user manual page for the Olya app covering all 13 sections (overview, authentication, profile setup, language selection, session management, transcript input with ASR engines, emotion/intent panel, belief state panel, pattern predictions, safety/quality metrics, strategy engine, session summary bar, and ethics/toxicity indicators), with a sticky toolbar containing a \"Download PDF\" button that triggers window.print(), a table of contents, and print-friendly layout.",
  "frontend/src/hooks/useTranslation.ts": "Custom hook that subscribes to `useAppLanguagePreference` and returns a `t(key)` function backed by `getTranslation()` from translations.ts, with automatic re-renders on language change and English fallback for missing keys.",
  "frontend/src/utils/emotionIntentSimulator.ts": "Emotion and intent analysis utility returning string arrays with a confidence map; accepts optional `detectedLanguage` and translates non-English text to English before pattern matching.",
  "frontend/src/i18n/translations.ts": "Complete translation dictionary covering all 41 supported app languages for every UI key (header, login, dashboard, language selector, panel, transcript, language names), using the key-first structure with `getTranslation(key, lang)` as the public API.",
  "frontend/src/hooks/useTheme.ts": "Custom hook that manages color theme and light/dark mode state using localStorage, applies the `dark` class to `document.documentElement` for Tailwind compatibility, and sets `data-theme` attributes for alternate color palettes.",
  "frontend/src/contexts/ThemeContext.tsx": "React context provider that wraps the `useTheme` hook and exposes theme state (currentTheme, currentMode, setTheme, toggleMode, setMode) globally via `useThemeContext`.",
  "frontend/src/components/ThemeSettingsPanel.tsx": "Panel component displaying a Light/Dark mode toggle switch with quick-select buttons and a 2×2 grid of color palette cards (Ocean Teal, Midnight Slate, Ember Amber, Forest Green) with color swatches and active-state highlighting."
}